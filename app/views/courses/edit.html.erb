<%tag = @course[:webinar]? "webinar" : "course"%>

<%= render partial: "courses/form", :locals => {title: t("#{tag}.edit"), button_name: t("course.update", course: @course), action: "/courses/#{@course[:id]}", course: @course, method: "put"} %>
